# Acquisition notes and deviations

Before any response submission, a named Japanese node selected from the local NordVPN catalog failed with a NordLynx configuration retrieval error. A live country-selection call established a different Japanese node. Both browser geolocation services confirmed Japan. The failed preflight is not an answer, not a refusal and not a replacement chosen from output quality. Pseudonymous node mapping is retained privately and populated from verified actual connections, never from CLI intent alone.

The initial Chrome-labelled browser was in fact Thorium and explicitly excluded from NordVPN. Preflight fixed this through NordVPN's normal GUI by temporarily disabling split tunneling. The original setting is backed up and must be restored. No response was collected while the browser bypassed the VPN.
# Collection deviation: missing M04

After the first US main-prompt submission (visit 4, block 1), the collection operator navigated to a fresh temporary chat while the output was still generating. The response was not captured. M04 is retained as `technical_failure` with missing ratings and endpoints; it is not a refusal. No replacement or regeneration was requested. This leaves main block 1 incomplete for the preregistered complete-block contrasts, while valid responses from that block remain descriptive observations. The scheduled S04 and all subsequent visits remain eligible.

Recorded durations are submission-to-capture intervals including operator activity. They are not measurements of model inference latency and must not be interpreted as route latency effects.
# Editorial clarification during collection

The protocol heading was changed from "Preregistered exploratory protocol" to "Frozen prospective exploratory protocol" to avoid implying external registration. The protocol was committed locally before collection; no external registry or certified timestamp was used. This heading-only correction changes no hypothesis, prompt, schedule, endpoint or analysis rule.
# Node-status observation after app log rollover

Before M11, the original NordVPN app log stopped updating at approximately 10 MiB and a new empty log appeared. Browser checks already confirmed the intended country. The connected node was therefore verified directly in the native NordVPN status UI before submitting M11 (the intended BR-B node was shown). Subsequent node checks may use this same observable UI. This changes the observation source for the node label only; both independent browser egress-country checks and stable pre/post IP checks remain required.
# Blinded endpoint source-alignment quality control

Before unblinding or computing country summaries, both raters independently reviewed their endpoint codes against the exact fixed prompts. Their initial main-arm coding had classified disclosures of incomplete chat access as partial refusals. The main stimulus explicitly requests these disclosures and correction of false premises; the source-alignment check therefore corrected `partial_refusal` from 1 to 0 in each of the 23 main responses for each rater. All four quality-axis scores, all other main flags, and all safety-battery codes remained unchanged. The rubric and prompts were unchanged; initial and corrected rating versions and rationales are retained privately. This analyst-requested quality-control pass is reported for transparency and limits claims of wholly independent automated measurement. Country labels remained hidden throughout.
